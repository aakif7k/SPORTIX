# Pinned, not floored. `fastapi>=0.111.0` meant every fresh install could differ
# from what was tested: CI resolved 0.141.1 / starlette 1.4.0 while development ran
# 0.111.0 / 0.37.2, and under the newer stack app.routes no longer flattens routes
# from included routers — so the route-table test saw 2 routes instead of 179. The
# Docker image installs from this file too, which means the container was running a
# stack the test suite had never exercised.
#
# Pinned to what a fresh install actually resolves, and verified there: development
# had been running 0.111.0 while CI and the Docker image both got 0.141.1, and
# 0.37.2 is no longer on the index so pinning backwards is not even installable.
# Raising this is fine, but re-run the suite and the live smoke first.
fastapi==0.141.1
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.9.0
pydantic-settings>=2.5.0
# Pinned below 16.0.0 deliberately -- see app/core/appwrite.py for the full note.
# 6.x sends a JSON body on GET requests, which Appwrite Cloud >= 1.8 rejects with
# 400 "request cannot have request body". 16.0.0+ switched to typed pydantic
# responses that nest document attributes under a "data" key instead of
# flattening them, which every service here would have to be rewritten for.
# 15.3.0 is the newest release that talks to current Cloud AND returns flat dicts.
appwrite>=15.3.0,<16.0.0

python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
httpx>=0.27.0
Pillow>=10.0.0
slowapi>=0.1.9
pytest>=8.2.0
pytest-asyncio>=0.23.6
aiofiles>=23.2.1
